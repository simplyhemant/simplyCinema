package com.simply.Cinema.core.location_and_venue.Enum;

public enum ScreenType {
    REGULAR,
    IMAX,
    THREE_D,       // Instead of 3D (invalid identifier)
    FOUR_DX,       // Instead of 4DX
    DOLBY_ATMOS
}
